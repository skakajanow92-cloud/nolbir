import 'package:flutter/material.dart';

class App096main extends StatelessWidget {
  const App096main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}