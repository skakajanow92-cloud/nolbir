import 'package:flutter/material.dart';

class App236main extends StatelessWidget {
  const App236main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}