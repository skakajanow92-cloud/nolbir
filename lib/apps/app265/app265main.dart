import 'package:flutter/material.dart';

class App265main extends StatelessWidget {
  const App265main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}