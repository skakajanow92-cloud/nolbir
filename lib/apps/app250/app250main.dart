import 'package:flutter/material.dart';

class App250main extends StatelessWidget {
  const App250main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}