import 'package:flutter/material.dart';

class App350main extends StatelessWidget {
  const App350main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}