import 'package:flutter/material.dart';

class App050main extends StatelessWidget {
  const App050main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}