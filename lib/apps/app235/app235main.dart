import 'package:flutter/material.dart';

class App235main extends StatelessWidget {
  const App235main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}