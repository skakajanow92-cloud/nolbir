import 'package:flutter/material.dart';

class App246main extends StatelessWidget {
  const App246main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}