import 'package:flutter/material.dart';

class App320main extends StatelessWidget {
  const App320main({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}