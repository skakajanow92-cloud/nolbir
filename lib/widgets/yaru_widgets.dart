import 'package:flutter/widgets.dart';

class YaruWidgets extends StatelessWidget {
  const YaruWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}